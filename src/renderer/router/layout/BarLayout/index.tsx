import { Box } from '@mui/material';
import React, { useCallback } from 'react';

const BarLayout = () => {
  const renderBar = useCallback(() => {

  },[]);

  return (
    <Box sx={{maxWidth: "60px", height:"100%", flex: "1 1 auto"}}>
      bar
    </Box>
  )
};

export default BarLayout;
